spring.application.name=auth-service

server.port=8082

# MongoDB configuration (with defaults and env overrides for Docker support)
spring.data.mongodb.uri=mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:wisdom_lms_auth}