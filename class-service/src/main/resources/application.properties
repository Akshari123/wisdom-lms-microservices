spring.application.name=class-service

server.port=${CLASS_SERVER_PORT:8084}

# MongoDB configuration (with defaults and env overrides for Docker support)
spring.data.mongodb.uri=${CLASS_MONGODB_URI:mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:classdb}}

app.api-key=${CLASS_API_KEY:${API_KEY:wisdom-lms-secret-key}}
