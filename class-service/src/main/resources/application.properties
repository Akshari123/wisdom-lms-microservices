spring.application.name=class-service

server.port=8084

# MongoDB configuration (with defaults and env overrides for Docker support)
spring.data.mongodb.uri=mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:classdb}

app.api-key=${API_KEY:wisdom-lms-secret-key}