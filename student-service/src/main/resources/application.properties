spring.application.name=student-service

server.port=${STUDENT_SERVER_PORT:8081}

# MongoDB configuration (with defaults and env overrides for Docker support)
spring.data.mongodb.uri=${STUDENT_MONGODB_URI:mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:studentdb}}

app.api-key=${STUDENT_API_KEY:${API_KEY:student-secret-2026}}
