spring.application.name=student-service

server.port=8081

# MongoDB configuration (with defaults and env overrides for Docker support)
spring.data.mongodb.uri=mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:studentdb}

app.api-key=${API_KEY:student-secret-2026}
