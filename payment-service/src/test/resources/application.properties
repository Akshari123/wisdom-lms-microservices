spring.application.name=payment-service-test

# Use MongoDB connection for tests
spring.data.mongodb.uri=mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:wisdom_payment_test}

# Custom API Key security setting for test environment
payment.api.key=wisdom-payment-key-2026
